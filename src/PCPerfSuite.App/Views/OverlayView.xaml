<UserControl x:Class="PCPerfSuite.App.Views.OverlayView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <ScrollViewer Padding="32,28,32,28">
        <StackPanel MaxWidth="820" HorizontalAlignment="Left">
            <TextBlock Text="Overlay en jeu" Style="{StaticResource H1}"/>
            <TextBlock Style="{StaticResource Caption}" Margin="0,4,0,20" TextWrapping="Wrap"
                       Text="Pousse CPU/GPU/RAM dans l'overlay de RTSS (RivaTuner Statistics Server) — le même moteur que MSI Afterburner, donc ça fonctionne aussi en plein écran exclusif. RTSS doit être installé et lancé (gratuit, guru3d.com) ; PCPerfSuite ne s'injecte dans aucun jeu, il écrit juste dans la mémoire partagée que RTSS relit."/>

            <Border Style="{StaticResource Card}" Margin="0,0,0,16">
                <StackPanel>
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>
                        <StackPanel Grid.Column="0">
                            <TextBlock Text="Activer l'overlay" Style="{StaticResource H2}"/>
                            <TextBlock Style="{StaticResource Caption}" Margin="0,4,0,0">
                                <Run Text="{Binding IsRtssDetected, Converter={StaticResource RtssStatus}}"/>
                            </TextBlock>
                        </StackPanel>
                        <CheckBox Grid.Column="1" Style="{StaticResource ToggleSwitch}" IsChecked="{Binding IsEnabled}" VerticalAlignment="Center"/>
                    </Grid>

                    <StackPanel Margin="0,18,0,0" Visibility="{Binding IsEnabled, Converter={StaticResource BoolToVisibility}}">
                        <TextBlock Text="Métriques affichées" Style="{StaticResource Caption}" Margin="0,0,0,8"/>
                        <StackPanel Orientation="Horizontal">
                            <CheckBox Content="CPU" IsChecked="{Binding ShowCpu}" Foreground="{StaticResource TextPrimaryBrush}" Margin="0,0,20,0" VerticalContentAlignment="Center"/>
                            <CheckBox Content="GPU" IsChecked="{Binding ShowGpu}" Foreground="{StaticResource TextPrimaryBrush}" Margin="0,0,20,0" VerticalContentAlignment="Center"/>
                            <CheckBox Content="RAM" IsChecked="{Binding ShowRam}" Foreground="{StaticResource TextPrimaryBrush}" VerticalContentAlignment="Center"/>
                        </StackPanel>
                    </StackPanel>
                </StackPanel>
            </Border>

            <Border Style="{StaticResource Card}" Visibility="{Binding IsEnabled, Converter={StaticResource BoolToVisibility}}">
                <StackPanel>
                    <TextBlock Text="Aperçu" Style="{StaticResource H2}"/>
                    <Border Background="Black" CornerRadius="6" Padding="14" Margin="0,10,0,0">
                        <TextBlock Text="{Binding PreviewText}" FontFamily="Consolas" FontSize="13" Foreground="#40FF40" TextWrapping="Wrap"/>
                    </Border>
                </StackPanel>
            </Border>
        </StackPanel>
    </ScrollViewer>
</UserControl>
